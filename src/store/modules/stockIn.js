
const state = {
    draftData: {
    },
}
// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
    setDraftData(state, data) {
        state.draftData = data
    }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}